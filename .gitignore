ScraperFC.egg-info
football_mcp.egg-info
football_data_mcp.egg-info
__pycache__
.ipynb_checkpoints
-checkpoint.ipynb
build/lib/ScraperFC/
.coverage
docs/build/
.tox/
prof/
docs/jupyter_execute/
uv.lock
__marimo__

# Football MCP — data folder (generated locally or served from R2; never commit)
data/
# Static FM / reference catalogues (small JSON — safe to commit)
!data/reference/
!data/reference/**

#Ignore environment files
.env

#Ignore .DS_Store files
.DS_Store

# Coding agent / local IDE noise (keep repo portable)
.cursor/
.claude/
.cursorrules

# Scraper and app logs (regenerated; can contain paths or timestamps you do not want in git)
*.log

# Python virtual environments (machine-specific; never commit)
.venv/
venv/
ENV/

# Test and type/lint tool caches (recreated on demand)
.pytest_cache/
htmlcov/
.mypy_cache/
.ruff_cache/

# Packaging build outputs (in addition to ScraperFC.egg-info above)
dist/
.eggs/

# Local FC25 / EAFC analysis (not for public repo)
reports/
scripts/analyze_fc25_column_coverage.py
test/test_eafc_ac_coalesce.py
test/test_stage1_attribute_scorer.py
scripts/
docs/
# Ignore local model checkpoints
checkpoints/